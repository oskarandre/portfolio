import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // output: "export",  // <=== enables static exports
  // distDir: "dist",  // <=== changes the build directory
  // reactStrictMode: true,
  // images: {
  //   unoptimized: true,
  // },
};

export default nextConfig;
